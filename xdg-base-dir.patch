diff --git a/Main/src/Application.cpp b/Main/src/Application.cpp
index 731573c2..28af00bb 100644
--- a/Main/src/Application.cpp
+++ b/Main/src/Application.cpp
@@ -941,41 +941,6 @@ bool Application::m_Init()
 {
 	ProfilerScope $("Application Setup");
 
-	String version = Utility::Sprintf("%d.%d.%d", VERSION_MAJOR, VERSION_MINOR, VERSION_PATCH);
-	Logf("Version: %s", Logger::Severity::Info, version.c_str());
-
-#ifdef EMBEDDED
-	Log("Embeedded version.");
-#endif
-
-#ifdef GIT_COMMIT
-	Logf("Git commit: %s", Logger::Severity::Info, GIT_COMMIT);
-#endif // GIT_COMMIT
-
-#ifdef _WIN32
-#ifdef CRASHDUMP
-	google_breakpad::CustomInfoEntry kCustomInfoEntries[]{
-		google_breakpad::CustomInfoEntry(L"version", std::wstring(version.begin(), version.end()).c_str()),
-#ifdef GIT_COMMIT
-		google_breakpad::CustomInfoEntry(L"git", L"" GIT_COMMIT),
-#else
-		CustomInfoEntry("git", ""),
-#endif
-	};
-	google_breakpad::CustomClientInfo custom_info = {kCustomInfoEntries, 2};
-	//CustomClientInfo custom_info
-	auto handler = new google_breakpad::ExceptionHandler(
-		L".\\crash_dumps",
-		NULL,
-		NULL,
-		NULL,
-		google_breakpad::ExceptionHandler::HANDLER_ALL,
-		MiniDumpNormal,
-		(const wchar_t *)nullptr,
-		&custom_info);
-#endif
-#endif
-
 	// Must have command line
 	assert(m_commandLine.size() >= 1);
 
@@ -994,9 +959,20 @@ bool Application::m_Init()
 
 	if (Path::gameDir.empty()) {
 		char* xdgDataDir = std::getenv("XDG_DATA_HOME");
+		String dataHome;
+
+		if (xdgDataDir && *xdgDataDir) {
+			dataHome = xdgDataDir;
+		} else {
+			// XDG Base Directory Specification: default to $HOME/.local/share
+			char* homeDir = std::getenv("HOME");
+			if (homeDir && *homeDir) {
+				dataHome = Utility::Sprintf("%s%c.local%cshare", homeDir, Path::sep, Path::sep);
+			}
+		}
 
-		if (xdgDataDir) {
-			String gameDir = Utility::Sprintf("%s%c%s", xdgDataDir, Path::sep, "unnamed-sdvx-clone");
+		if (!dataHome.empty()) {
+			String gameDir = Utility::Sprintf("%s%c%s", dataHome, Path::sep, "unnamed-sdvx-clone");
 
 			auto gameDataSourceDir = Path::RemoveLast(Path::GetExecutablePath());
 			String xdgDataDirs(std::getenv("XDG_DATA_DIRS"));
@@ -1044,6 +1020,41 @@ bool Application::m_Init()
 		}
 	}
 
+	String version = Utility::Sprintf("%d.%d.%d", VERSION_MAJOR, VERSION_MINOR, VERSION_PATCH);
+	Logf("Version: %s", Logger::Severity::Info, version.c_str());
+
+#ifdef EMBEDDED
+	Log("Embeedded version.");
+#endif
+
+#ifdef GIT_COMMIT
+	Logf("Git commit: %s", Logger::Severity::Info, GIT_COMMIT);
+#endif // GIT_COMMIT
+
+#ifdef _WIN32
+#ifdef CRASHDUMP
+	google_breakpad::CustomInfoEntry kCustomInfoEntries[]{
+		google_breakpad::CustomInfoEntry(L"version", std::wstring(version.begin(), version.end()).c_str()),
+#ifdef GIT_COMMIT
+		google_breakpad::CustomInfoEntry(L"git", L"" GIT_COMMIT),
+#else
+		CustomInfoEntry("git", ""),
+#endif
+	};
+	google_breakpad::CustomClientInfo custom_info = {kCustomInfoEntries, 2};
+	//CustomClientInfo custom_info
+	auto handler = new google_breakpad::ExceptionHandler(
+		L".\\crash_dumps",
+		NULL,
+		NULL,
+		NULL,
+		google_breakpad::ExceptionHandler::HANDLER_ALL,
+		MiniDumpNormal,
+		(const wchar_t *)nullptr,
+		&custom_info);
+#endif
+#endif
+
 	// Set the locale so that functions such as `fopen` use UTF-8.
 	{
 		String prevLocale = setlocale(LC_CTYPE, nullptr);
diff --git a/Main/src/ChallengeResult.cpp b/Main/src/ChallengeResult.cpp
index ff34c00b..40395e8a 100644
--- a/Main/src/ChallengeResult.cpp
+++ b/Main/src/ChallengeResult.cpp
@@ -435,7 +435,7 @@ public:
 		}
 		Vector2i size(w, h);
 		Image screenshot = ImageRes::Screenshot(g_gl, size, { x,y });
-		String screenshotPath = "screenshots/" + Shared::Time::Now().ToString() + ".png";
+		String screenshotPath = Path::Absolute("screenshots/" + Shared::Time::Now().ToString() + ".png");
 		if (screenshot.get() != nullptr)
 		{
 			screenshot->SavePNG(screenshotPath);
